% AUTO-GENERATED by scripts/build.py from data/theme.yaml — do not hand-edit.
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{theme}
\RequirePackage{xcolor}
\RequirePackage{fontspec}

\definecolor{graphite900}{RGB}{28,31,34}
\definecolor{steel700}{RGB}{61,70,80}
\definecolor{slate400}{RGB}{124,136,148}
\definecolor{fog100}{RGB}{237,239,241}
\definecolor{paper}{RGB}{255,255,255}
\definecolor{blueprint500}{RGB}{35,87,137}
\definecolor{signalamber}{RGB}{217,142,59}

% --- fonts: fall back to a widely-installed substitute if the
% theme font isn't installed on this machine, so text is never
% silently rendered invisible (fontspec's default failure mode).
% Install the real fonts for the intended look — see README.md.
\IfFontExistsTF{Archivo}{\newfontfamily\displayfont{Archivo}}{\newfontfamily\displayfont{DejaVu Sans}\ClassWarning{theme}{'Archivo' not found, falling back to DejaVu Sans}}
\IfFontExistsTF{IBM Plex Mono}{\newfontfamily\monofont{IBM Plex Mono}}{\newfontfamily\monofont{DejaVu Sans Mono}\ClassWarning{theme}{'IBM Plex Mono' not found, falling back to DejaVu Sans Mono}}
\IfFontExistsTF{IBM Plex Sans}{\setmainfont{IBM Plex Sans}}{\setmainfont{DejaVu Sans}\ClassWarning{theme}{'IBM Plex Sans' not found, falling back to DejaVu Sans}}

% --- type scale (from data/theme.yaml type_scale:) — LaTeX size commands ---
\newcommand{\textHeroTitle}{\Huge}
\newcommand{\textProjectTitle}{\LARGE}
\newcommand{\textSectionHeading}{\Large}
\newcommand{\textMetaLabel}{\normalsize}
\newcommand{\textBody}{\normalsize}
\newcommand{\textCaption}{\small}
\newcommand{\textSmall}{\footnotesize}

% --- media framing (from data/theme.yaml media:) — computed here
% so portfolio-macros.sty needs no runtime conditionals for the
% border_enabled on/off toggle. Per-block background/border
% overrides are a website-only feature (see README); the PDF
% always uses these theme-wide defaults.
\newcommand{\mediaFrameColor}{fog100}
\newcommand{\mediaBorderColor}{fog100}
\newcommand{\mediaBorderWidthVal}{0pt}
